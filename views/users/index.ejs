<%- include('../partials/header') %>


<% users.forEach(function(u) { %>
<div class="box flex">
  <img class="profile-pic" src="<%= u.avatar %>" />
  <div class="inner-space center">
  <h1><%= u.name %></h1>
  <a href="/users/<%= u._id %>">View Profile</a>
  </div>
</div>
  <% }) %>
<%- include('../partials/footer') %>
